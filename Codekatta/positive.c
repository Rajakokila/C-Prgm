#include <stdio.h>
int main(void)
{
 int a,b;
 printf("Enter the number");
 scanf("%d%d",&a,&b);
   {
     if(a>0)
     {
       printf("positive");
     }
     elseif(a<0)
     {
       printf("Negative");
     }
     else
     {
       printf("Zero");
     } 
   }
 return 0;
}
